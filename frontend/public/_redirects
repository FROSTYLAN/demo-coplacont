# Netlify redirects for React Router
# This ensures that all routes are handled by React Router instead of Netlify's server

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Optional: Redirect API calls to your backend if needed
# /api/*  https://your-backend-url.com/api/:splat  200

# Optional: Handle specific file extensions that should not be redirected
# *.js    /static/js/:splat   200
# *.css   /static/css/:splat  200
# *.png   /static/media/:splat 200
# *.jpg   /static/media/:splat 200
# *.svg   /static/media/:splat 200